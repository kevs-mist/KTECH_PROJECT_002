import { NextResponse } from "next/server"; import { adminAuth } from "../../../../utils/firebase/admin"; import { createAdminClient } from "../../../../utils/supabase/admin"; import {     assertSameOrigin,     checkRateLimit,     getBearerToken,     getClientIp,     jsonError,     rateLimitResponse, } from "../../../src/lib/server/apiSecurity";  export async function POST(request: Request) {     try {         assertSameOrigin(request);          const limit = checkRateLimit({             key: `register:${getClientIp(request)}`,             limit: 5,             windowMs: 60 * 60 * 1000,         });         if (!limit.success) return rateLimitResponse(limit.resetAt);          const token = getBearerToken(request);         if (!token) {             return NextResponse.json({ error: "Unauthorized" }, { status: 401 });         }          const decodedToken = await adminAuth.verifyIdToken(token);         const uid = decodedToken.uid;         const email = decodedToken.email;          if (!email) {             return NextResponse.json({ error: "Email is required for registration" }, { status: 400 });         }          const body = await request.json();         const name = String(body.name ?? "").trim();         const isAdminRequested = !!body.isAdminRequested;          if (!name || name.length < 2) {             return NextResponse.json({ error: "Full name is required (minimum 2 characters)" }, { status: 400 });         }          const allowedDomains = process.env.ALLOWED_EMAIL_DOMAINS             ?.split(",")             .map((domain) => domain.trim().toLowerCase())             .filter(Boolean) ?? [];          if (allowedDomains.length > 0) {             const emailDomain = email.split("@")[1]?.toLowerCase();             if (!emailDomain || !allowedDomains.includes(emailDomain)) {                 return NextResponse.json(                     { error: "Registration restricted to approved company email addresses." },                     { status: 403 }                 );             }         }          const supabase = createAdminClient();         const { data: existingUser } = await supabase             .from("users")             .select("firebase_uid")             .eq("firebase_uid", uid)             .maybeSingle();          if (existingUser) {             return NextResponse.json({ error: "This account is already registered." }, { status: 409 });         }          const role: "employee" | "user" = isAdminRequested ? "user" : "employee";         const { error: userError } = await supabase.from("users").insert([             {                 firebase_uid: uid,                 email,                 full_name: name,                 role,                 created_at: new Date().toISOString(),             },         ]);          if (userError) throw userError;          if (role === "employee") {             const { error: empError } = await supabase.from("employees").insert([                 {                     firebase_uid: uid,                     employee_id: `EMP-${uid.slice(0, 8).toUpperCase()}`,                     status: "active",                     joined_at: new Date().toISOString(),                 },             ]);              if (empError) {                 await supabase.from("users").delete().eq("firebase_uid", uid);                 throw empError;             }         }          if (isAdminRequested) {             const { error: reqErr } = await supabase                 .from("admin_requests")                 .insert([{ firebase_uid: uid, email, status: "pending" }]);              if (reqErr) {                 await supabase.from("users").delete().eq("firebase_uid", uid);                 throw reqErr;             }         }          return NextResponse.json({ success: true, role }, { status: 201 });     } catch (error: unknown) {         console.error("Registration API error:", error);         return jsonError(error, "Registration failed");     } }